{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    <div>
        <a href="{{ admin.generateUrl('list', {'parent_id':object.id}) }}">{{ object.title }}</a>
    </div>
{% endblock %}